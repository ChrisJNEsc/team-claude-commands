# LiveTrace - Install Dependencies

Install required Python dependencies for the LiveTrace Chrome monitoring tool.

## Run this command:

```bash
pip3 install requests websocket-client
```

After installation completes, you can use `/team:livetrace` to start monitoring Chrome sessions.
